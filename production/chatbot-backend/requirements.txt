httpx>=0.27.0            # chamadas HTTP: Ollama (embedding), sacf-ai-worker, API do Telegram
psycopg[binary,pool]>=3.2.0  # driver Postgres + pool de conexões
alembic>=1.13            # migrations do schema (memória de conversa); app segue em psycopg cru
pgvector>=0.3.0          # adaptador do tipo vector para o psycopg
python-dotenv>=1.0.0     # carrega o .env
langid>=1.1.6            # detecção de idioma: fallback offline se a classificação via LLM falhar (ver bot/language.py)
